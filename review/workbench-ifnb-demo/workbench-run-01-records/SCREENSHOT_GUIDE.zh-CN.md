# 四张真实截图的取景建议

目前未生成或伪造宿主截图。请在当前真实 Codex 任务中用系统截图功能采集；保留未经改写的原图。下面的文件名是建议保存名，不是已经存在的截图路径。

## 1 — 问题、计划和真实批准

建议名：01-plan-and-approval.png。

在当前任务滚动到用户“批准按刚才列出的六步计划”消息及相邻计划区域。取景保留宿主名称/任务上下文，并清楚显示 8 位配对供者、16 个伪 bulk 样本、`~ donor + condition` 和批准仅限探索性执行。若一屏放不下，优先保留真实批准消息与右侧打开的已保存计划，不把说明文件伪装为原对话。

## 2 — 实际执行与插件证据

建议名：02-execution-and-literature.png。

左侧取当前任务中分析命令成功完成的工具结果；右侧打开 `host-execution.md` 或 `commands.json`，露出 workbench-run-01、起止时间和 exit_code=0。另使 Literature 成功返回的 PMID 29227470 / DOI 可见。若界面仅能显示保存文件，请同时保留文件名与路径，明确这是保存的返回，不是虚构的 MCP 卡片。图注写“NGS 技能用于理解/设计；Python 执行；Literature 技能脚本检索”。

## 3 — 新生成的科学结果

建议名：03-new-result-figure.png。

打开 `workbench-run-01/showcase_figure.png`，以完整三联图为主体，保留文件名或路径。左侧标题必须完整显示 Author t-SNE；中间四个基因与配对线、右侧火山图坐标轴均清晰。不要裁掉作者 t-SNE 标识，不把图说成新 UMAP。图注注明：5,385 个来源注释 CD14+ 细胞、8 对供者、条件与文库混杂。

## 4 — 可信边界与真实审计状态

建议名：04-audit-and-limitations.png。

打开 `SHOWCASE.zh-CN.md` 的“限制与审计结论”及原始 `analysis_manifest.json` 的 bionexus_identity_audit 区域，清楚呈现 TENTATIVE、线粒体 NOT_ASSESSED、条件/文库混杂与人工裁决待完成。保留 `host-execution.md` 中 PERMITTED/ABSTAIN 各自适用阶段的说明。若展示完成后 SUPPORTED，必须连同其限定关联范围呈现；不得只截取一个绿色状态。

拍摄后将四个实际路径填入 `host-execution.md`。截图证明界面中发生或展示了什么，不证明独立科学有效性。参赛回复草稿尚未发布。
